import java.util.HashMap;
import java.util.Map;

public class pairs {

}

public int pairs(String f , String str , String l ) {

    Map<String, String> pairs = new HashMap<>();

    pairs.put(W, 0);

    for (String srtf = f; str.length - 1; i++) {

        if (String pairs:
        str()){

            return f;

            for (String srtl = l; str.length - 1; i++) {

                if (String pairs:
                str()){

                    return l;
                }

                return pairs;
            }
        }
    }
}